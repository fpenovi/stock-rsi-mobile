import { palette } from 'config/theme';

export const headerStyle = {
  backgroundColor: palette.primaryDark
};

export const headerTitleContainerStyle = { justifyContent: 'center' };
export const headerTintColor = palette.primaryAccent;
